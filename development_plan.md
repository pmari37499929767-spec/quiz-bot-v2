# ПЛАН РАЗРАБОТКИ КВИЗ-БОТА: ДЕТАЛЬНАЯ РАЗБИВКА

## SPRINT 0: Подготовка (1-2 дня)

### День 1: Контент и сценарий

#### Задача 0.1: Определение ниши и аудитории
**Время**: 2 часа
**Чеклист**:
- [ ] Уточнить нишу эксперта (бизнес/здоровье/финансы)
- [ ] Определить целевую аудиторию
- [ ] Составить портрет клиента
- [ ] Определить главные боли ЦА

**Результат**: Документ с описанием ниши и ЦА

---

#### Задача 0.2: Написание сценария квиза
**Время**: 4 часа
**Чеклист**:
- [ ] Написать ХУК (приветствие)
- [ ] Придумать 5-7 вопросов
- [ ] Для каждого вопроса: 3-4 варианта ответа
- [ ] Прописать логику скоринга (баллы по категориям)
- [ ] Написать 4 варианта результатов
- [ ] Придумать ТВИСТ (промежуточный инсайт)

**Шаблон вопроса**:
```
Вопрос №_: [Текст вопроса]

Варианты ответов:
1. [Текст] → Баллы: product:_, traffic:_, sales:_, system:_
2. [Текст] → Баллы: product:_, traffic:_, sales:_, system:_
3. [Текст] → Баллы: product:_, traffic:_, sales:_, system:_
```

**Результат**: Google Doc со сценарием

---

#### Задача 0.3: Подготовка оффера для лид-формы
**Время**: 2 часа
**Чеклист**:
- [ ] Определить, что предлагаем (консультация/разбор/курс)
- [ ] Написать описание оффера (3-5 пунктов)
- [ ] Указать цену/скидку
- [ ] Придумать бонус (чек-лист/гайд)
- [ ] Написать текст лид-формы

**Результат**: Текст оффера и лид-формы

---

### День 2: Техническая подготовка

#### Задача 0.4: Создание Telegram-бота
**Время**: 30 минут
**Чеклист**:
- [ ] Открыть @BotFather в Telegram
- [ ] Выполнить команду `/newbot`
- [ ] Придумать имя бота (например: "Новогодний Детектив")
- [ ] Придумать username (например: @newyear_detective_bot)
- [ ] Сохранить API токен в безопасное место
- [ ] Установить описание бота `/setdescription`
- [ ] Установить команды `/setcommands`:
  ```
  start - Начать расследование
  stats - Статистика (только для админа)
  ```

**Результат**: Работающий бот с токеном

---

#### Задача 0.5: Настройка окружения разработки
**Время**: 1 час
**Чеклист**:
- [ ] Создать репозиторий на GitHub
- [ ] Склонировать на локальную машину
- [ ] Создать виртуальное окружение Python:
  ```bash
  python3 -m venv venv
  source venv/bin/activate  # Linux/Mac
  venv\Scripts\activate     # Windows
  ```
- [ ] Создать `requirements.txt`:
  ```
  aiogram==3.3.0
  python-dotenv==1.0.0
  sqlalchemy==2.0.25
  asyncpg==0.29.0
  aiosqlite==0.19.0
  ```
- [ ] Установить зависимости: `pip install -r requirements.txt`
- [ ] Создать `.env` файл:
  ```
  BOT_TOKEN=ваш_токен
  OWNER_ID=ваш_telegram_id
  DATABASE_URL=sqlite+aiosqlite:///./quiz_bot.db
  ```
- [ ] Добавить `.env` в `.gitignore`

**Результат**: Готовое окружение для разработки

---

#### Задача 0.6: Проектирование структуры
**Время**: 1 час
**Чеклист**:
- [ ] Создать структуру папок:
  ```
  quiz_bot/
  ├── config.py
  ├── main.py
  ├── requirements.txt
  ├── .env
  ├── handlers/
  │   ├── __init__.py
  │   ├── start.py
  │   ├── quiz.py
  │   ├── results.py
  │   └── lead_form.py
  ├── models/
  │   ├── __init__.py
  │   ├── user.py
  │   └── quiz_session.py
  ├── database/
  │   ├── __init__.py
  │   └── db.py
  ├── content/
  │   ├── __init__.py
  │   ├── questions.py
  │   └── results_logic.py
  └── utils/
      ├── __init__.py
      ├── keyboards.py
      └── notifications.py
  ```
- [ ] Создать `__init__.py` во всех папках
- [ ] Нарисовать блок-схему флоу (можно на бумаге/Excalidraw)

**Результат**: Структура проекта

---

## SPRINT 1: Базовый функционал (3-4 дня)

### День 3: Конфигурация и база данных

#### Задача 1.1: Конфигурация приложения
**Время**: 1 час
**Файл**: `config.py`
**Код**:
```python
import os
from dotenv import load_dotenv

load_dotenv()

# Telegram
BOT_TOKEN = os.getenv('BOT_TOKEN')
OWNER_ID = int(os.getenv('OWNER_ID'))

# База данных
DATABASE_URL = os.getenv('DATABASE_URL', 'sqlite+aiosqlite:///./quiz_bot.db')

# Настройки
QUESTIONS_COUNT = 5  # Количество вопросов
RESULT_CATEGORIES = ['product', 'traffic', 'sales', 'system']
```

**Чеклист**:
- [ ] Создать файл
- [ ] Протестировать загрузку переменных
- [ ] Проверить, что токен читается

---

#### Задача 1.2: Модели данных
**Время**: 2 часа
**Файл**: `models/user.py`, `models/quiz_session.py`

**Чеклист**:
- [ ] Создать базовый класс `Base`
- [ ] Модель `User` с полями:
  - id, telegram_id, username, first_name, created_at
- [ ] Модель `QuizSession` с полями:
  - id, user_id, started_at, completed_at, answers (JSON), score (JSON), result_category
- [ ] Модель `Lead` с полями:
  - id, user_id, session_id, phone, email, comment, created_at

**Код** (models/user.py):
```python
from datetime import datetime
from sqlalchemy import Column, Integer, BigInteger, String, DateTime
from sqlalchemy.orm import declarative_base

Base = declarative_base()

class User(Base):
    __tablename__ = 'users'
    
    id = Column(Integer, primary_key=True)
    telegram_id = Column(BigInteger, unique=True, nullable=False)
    username = Column(String(255))
    first_name = Column(String(255))
    created_at = Column(DateTime, default=datetime.now)
```

**Результат**: Готовые модели

---

#### Задача 1.3: Подключение к БД
**Время**: 1 час
**Файл**: `database/db.py`

**Чеклист**:
- [ ] Создать асинхронный engine
- [ ] Создать async_sessionmaker
- [ ] Функция `init_db()` для создания таблиц
- [ ] Функция `get_session()` для получения сессии

**Код**:
```python
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
from config import DATABASE_URL
from models.user import Base

engine = create_async_engine(DATABASE_URL, echo=True)
async_session = async_sessionmaker(engine, expire_on_commit=False)

async def init_db():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)

async def get_session():
    async with async_session() as session:
        yield session
```

**Результат**: Рабочее подключение к БД

---

### День 4: Обработчик старта и регистрация

#### Задача 1.4: Хендлер /start
**Время**: 2 часа
**Файл**: `handlers/start.py`

**Чеклист**:
- [ ] Создать роутер
- [ ] Обработчик команды `/start`
- [ ] Регистрация пользователя в БД (если новый)
- [ ] Приветственное сообщение (ХУК)
- [ ] Inline-кнопка "Начать расследование"

**Код**:
```python
from aiogram import Router, F
from aiogram.filters import CommandStart
from aiogram.types import Message, InlineKeyboardMarkup, InlineKeyboardButton

router = Router()

@router.message(CommandStart())
async def cmd_start(message: Message):
    # Регистрация пользователя
    # ...
    
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="🎄 Начать расследование", callback_data="start_quiz")]
    ])
    
    text = """
🔍 <b>Детектив по итогам года готов к расследованию!</b>

2025 подходит к концу. Кто-то заработал больше, кто-то меньше. 
Но главный вопрос: <b>ЧТО именно помешало вам утроить результат?</b>

Я найду узкое место за 2 минуты. Готовы?
    """
    
    await message.answer(text, reply_markup=keyboard, parse_mode='HTML')
```

**Результат**: Работающий /start

---

#### Задача 1.5: Сохранение пользователей в БД
**Время**: 1 час

**Чеклист**:
- [ ] Функция `get_or_create_user()`
- [ ] Проверка существования по telegram_id
- [ ] Создание нового пользователя
- [ ] Возврат объекта User

**Код**:
```python
async def get_or_create_user(telegram_id, username, first_name):
    async with get_session() as session:
        user = await session.execute(
            select(User).where(User.telegram_id == telegram_id)
        )
        user = user.scalar_one_or_none()
        
        if not user:
            user = User(
                telegram_id=telegram_id,
                username=username,
                first_name=first_name
            )
            session.add(user)
            await session.commit()
        
        return user
```

**Результат**: Пользователи сохраняются

---

### День 5: Логика квиза

#### Задача 1.6: Банк вопросов
**Время**: 1 час
**Файл**: `content/questions.py`

**Чеклист**:
- [ ] Перенести вопросы из сценария в Python
- [ ] Структура: список словарей
- [ ] Каждый вопрос: id, text, type, options
- [ ] Каждый option: text, score

**Результат**: Файл с вопросами

---

#### Задача 1.7: FSM States для квиза
**Время**: 1 час
**Файл**: `handlers/quiz.py`

**Чеклист**:
- [ ] Создать StatesGroup для квиза
- [ ] States: waiting_for_name, in_quiz
- [ ] Хранение в state: current_question, answers, score

**Код**:
```python
from aiogram.fsm.state import State, StatesGroup

class QuizStates(StatesGroup):
    waiting_for_name = State()
    in_quiz = State()
```

**Результат**: States готовы

---

#### Задача 1.8: Запрос имени
**Время**: 1 час

**Чеклист**:
- [ ] Callback на "start_quiz"
- [ ] Запрос имени пользователя
- [ ] Переход в state waiting_for_name
- [ ] Сохранение имени

**Код**:
```python
@router.callback_query(F.data == "start_quiz")
async def start_quiz_handler(callback: CallbackQuery, state: FSMContext):
    await callback.message.answer(
        "Прежде чем начнём расследование, представьтесь:\n\n"
        "Как вас зовут?"
    )
    await state.set_state(QuizStates.waiting_for_name)

@router.message(QuizStates.waiting_for_name)
async def name_received(message: Message, state: FSMContext):
    name = message.text
    await state.update_data(
        name=name,
        current_question=0,
        answers={},
        score={'product': 0, 'traffic': 0, 'sales': 0, 'system': 0}
    )
    await state.set_state(QuizStates.in_quiz)
    await send_question(message, state, 0)
```

**Результат**: Имя запрашивается и сохраняется

---

#### Задача 1.9: Показ вопросов
**Время**: 2 часа

**Чеклист**:
- [ ] Функция `send_question(message, state, question_id)`
- [ ] Получение вопроса из банка
- [ ] Создание inline-клавиатуры с вариантами
- [ ] Отправка сообщения

**Код**:
```python
async def send_question(message: Message, state: FSMContext, question_id: int):
    question = QUESTIONS[question_id]
    
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(
            text=opt['text'], 
            callback_data=f"ans_{question_id}_{i}"
        )]
        for i, opt in enumerate(question['options'])
    ])
    
    progress = f"Вопрос {question_id + 1} из {len(QUESTIONS)}"
    text = f"<b>{progress}</b>\n\n{question['text']}"
    
    await message.answer(text, reply_markup=keyboard, parse_mode='HTML')
```

**Результат**: Вопросы показываются

---

### День 6: Обработка ответов и скоринг

#### Задача 1.10: Обработка ответов
**Время**: 2 часа

**Чеклист**:
- [ ] Callback на паттерн "ans_*"
- [ ] Парсинг question_id и option_id
- [ ] Сохранение ответа в state
- [ ] Добавление баллов к score
- [ ] Переход к следующему вопросу или результату

**Код**:
```python
@router.callback_query(F.data.startswith("ans_"))
async def handle_answer(callback: CallbackQuery, state: FSMContext):
    await callback.answer()
    
    _, q_id, opt_id = callback.data.split("_")
    q_id, opt_id = int(q_id), int(opt_id)
    
    data = await state.get_data()
    answers = data.get('answers', {})
    score = data.get('score')
    
    # Сохраняем ответ
    answers[q_id] = opt_id
    
    # Добавляем баллы
    question = QUESTIONS[q_id]
    option = question['options'][opt_id]
    for category, points in option['score'].items():
        score[category] += points
    
    await state.update_data(answers=answers, score=score)
    
    # Следующий вопрос
    next_q = q_id + 1
    if next_q < len(QUESTIONS):
        await send_question(callback.message, state, next_q)
    else:
        # Показываем промежуточный инсайт (ТВИСТ)
        await show_twist(callback.message, state)
```

**Результат**: Ответы обрабатываются, баллы начисляются

---

#### Задача 1.11: Логика определения результата
**Время**: 2 часа
**Файл**: `content/results_logic.py`

**Чеклист**:
- [ ] Функция `determine_bottleneck(score)`
- [ ] Определение категории с max баллами
- [ ] Возврат словаря с title, description, steps
- [ ] 4 варианта результатов

**Код**:
```python
def determine_bottleneck(score: dict) -> dict:
    max_category = max(score, key=score.get)
    
    results = {
        'product': {
            'emoji': '💡',
            'title': 'ПРОДУКТ',
            'description': 'Ваше узкое место — нечёткое позиционирование...',
            'steps': [
                'Выберите ОДИН флагманский продукт',
                'Пропишите уникальность',
                'Соберите 10 кейсов'
            ]
        },
        # ... остальные категории
    }
    
    return {
        'category': max_category,
        **results[max_category]
    }
```

**Результат**: Логика скоринга работает

---

### День 7: Результаты и твист

#### Задача 1.12: Промежуточный инсайт (ТВИСТ)
**Время**: 1 час
**Файл**: `handlers/results.py`

**Чеклист**:
- [ ] Функция `show_twist()`
- [ ] Короткое интригующее сообщение
- [ ] Кнопка "Показать результат"

**Код**:
```python
async def show_twist(message: Message, state: FSMContext):
    data = await state.get_data()
    name = data['name']
    
    text = f"""
🔍 <b>Стоп! Я вижу закономерность...</b>

{name}, анализ ваших ответов показывает интересную картину.
Сейчас я расскажу, что именно тормозит ваш рост.

Готовы увидеть правду?
    """
    
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="Да, показывай результат 🎯", callback_data="show_result")]
    ])
    
    await message.answer(text, reply_markup=keyboard, parse_mode='HTML')
```

**Результат**: Твист работает

---

#### Задача 1.13: Показ результата
**Время**: 2 часа

**Чеклист**:
- [ ] Callback на "show_result"
- [ ] Получение score из state
- [ ] Вызов `determine_bottleneck()`
- [ ] Форматирование результата
- [ ] Кнопки: "Хочу разбор" / "Нет, спасибо"

**Код**:
```python
@router.callback_query(F.data == "show_result")
async def show_result_handler(callback: CallbackQuery, state: FSMContext):
    await callback.answer()
    
    data = await state.get_data()
    score = data['score']
    name = data['name']
    
    result = determine_bottleneck(score)
    
    text = f"""
{result['emoji']} <b>РЕЗУЛЬТАТ РАССЛЕДОВАНИЯ</b>

{name}, ваше узкое место: <b>{result['title']}</b>

{result['description']}

<b>Что делать дальше:</b>
{''.join([f'✅ {step}' for step in result['steps']])}

---

<i>P.S. Хотите разобрать вашу ситуацию детально?</i>
    """
    
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="💎 Да, хочу разбор", callback_data="want_consultation")],
        [InlineKeyboardButton(text="Нет, спасибо", callback_data="no_thanks")]
    ])
    
    await callback.message.answer(text, reply_markup=keyboard, parse_mode='HTML')
    
    # Сохраняем результат в БД
    await save_quiz_result(data, result)
```

**Результат**: Результат показывается

---

## SPRINT 2: Лид-форма и интеграции (2-3 дня)

### День 8: Лид-форма

#### Задача 2.1: FSM для лид-формы
**Время**: 1 час
**Файл**: `handlers/lead_form.py`

**Чеклист**:
- [ ] States: waiting_for_phone, waiting_for_comment
- [ ] Callback на "want_consultation"
- [ ] Показ оффера

**Код**:
```python
class LeadFormStates(StatesGroup):
    waiting_for_phone = State()
    waiting_for_comment = State()

@router.callback_query(F.data == "want_consultation")
async def start_lead_form(callback: CallbackQuery, state: FSMContext):
    text = """
💎 <b>Отлично! Я приглашаю вас на персональную консультацию:</b>

✅ 60 минут индивидуального разбора
✅ Конкретный план действий на Q1 2025
✅ Бонус: чек-лист "Точки роста бизнеса"

Цена: 5000₽ → Для участников квиза: <b>3000₽</b>

Как с вами связаться? Введите номер телефона:
    """
    
    await callback.message.answer(text, parse_mode='HTML')
    await state.set_state(LeadFormStates.waiting_for_phone)
```

**Результат**: Оффер показывается

---

#### Задача 2.2: Сбор телефона
**Время**: 1 час

**Чеклист**:
- [ ] Handler для waiting_for_phone
- [ ] Валидация формата телефона
- [ ] Сохранение в state
- [ ] Запрос комментария

**Код**:
```python
import re

@router.message(LeadFormStates.waiting_for_phone)
async def phone_received(message: Message, state: FSMContext):
    phone = message.text.strip()
    
    # Простая валидация
    if not re.match(r'^[\d\+\-\(\) ]{10,}$', phone):
        await message.answer(
            "❌ Некорректный формат телефона. Попробуйте ещё раз:"
        )
        return
    
    await state.update_data(phone=phone)
    
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="Пропустить", callback_data="skip_comment")]
    ])
    
    await message.answer(
        "Отлично! Последний вопрос:\n\n"
        "Есть ли что-то важное, что мне нужно знать о вашей ситуации?",
        reply_markup=keyboard
    )
    await state.set_state(LeadFormStates.waiting_for_comment)
```

**Результат**: Телефон собирается

---

#### Задача 2.3: Сбор комментария и сохранение
**Время**: 2 часа

**Чеклист**:
- [ ] Handler для комментария
- [ ] Handler для "Пропустить"
- [ ] Сохранение лида в БД
- [ ] Благодарственное сообщение

**Код**:
```python
@router.message(LeadFormStates.waiting_for_comment)
async def comment_received(message: Message, state: FSMContext):
    comment = message.text
    await save_lead(state, comment)
    await send_thank_you(message, state)

@router.callback_query(F.data == "skip_comment")
async def skip_comment(callback: CallbackQuery, state: FSMContext):
    await save_lead(state, comment=None)
    await send_thank_you(callback.message, state)

async def save_lead(state: FSMContext, comment: str = None):
    data = await state.get_data()
    
    # Сохранение в БД
    lead = Lead(
        user_id=data['user_id'],
        session_id=data['session_id'],
        name=data['name'],
        phone=data['phone'],
        comment=comment
    )
    # ... save to DB
    
    # Уведомление владельцу
    await notify_owner(data, comment)
    
    await state.clear()

async def send_thank_you(message: Message, state: FSMContext):
    text = """
✅ <b>Заявка принята!</b>

Я свяжусь с вами в течение 24 часов.

А пока можете:
📱 Подписаться на мой канал: @your_channel
📧 Почитать статьи: your-site.com
    """
    
    await message.answer(text, parse_mode='HTML')
```

**Результат**: Лид сохраняется

---

### День 9: Уведомления и админка

#### Задача 2.4: Уведомление владельцу
**Время**: 1 час
**Файл**: `utils/notifications.py`

**Чеклист**:
- [ ] Функция `notify_owner()`
- [ ] Форматированное сообщение
- [ ] Отправка через bot.send_message()

**Код**:
```python
from datetime import datetime
from config import OWNER_ID

async def notify_owner(bot, lead_data: dict):
    message = f"""
🔥 <b>НОВАЯ ЗАЯВКА!</b>

👤 Имя: {lead_data['name']}
📱 Telegram: @{lead_data['username']}
☎️ Телефон: {lead_data['phone']}

📊 Результат квиза: {lead_data['bottleneck']}

💬 Комментарий:
{lead_data.get('comment', 'нет')}

---
🕐 Дата: {datetime.now().strftime('%d.%m.%Y %H:%M')}
    """
    
    await bot.send_message(OWNER_ID, message, parse_mode='HTML')
```

**Результат**: Уведомления приходят

---

#### Задача 2.5: Команда /stats (админка)
**Время**: 2 часа

**Чеклист**:
- [ ] Проверка, что пользователь = владелец
- [ ] Подсчёт статистики из БД
- [ ] Форматирование и отправка

**Код**:
```python
@router.message(Command("stats"))
async def cmd_stats(message: Message):
    if message.from_user.id != OWNER_ID:
        return
    
    stats = await get_stats_from_db()
    
    text = f"""
📊 <b>СТАТИСТИКА БОТА</b>

👥 Всего пользователей: {stats['total_users']}
📝 Прохождений квиза: {stats['total_sessions']}
✅ Завершено: {stats['completed_sessions']}
💰 Заявок: {stats['total_leads']}

🎯 Конверсия:
• Старт → Завершение: {stats['cr1']:.1%}
• Завершение → Заявка: {stats['cr2']:.1%}

📈 Результаты:
• Продукт: {stats['results']['product']}
• Трафик: {stats['results']['traffic']}
• Продажи: {stats['results']['sales']}
• Система: {stats['results']['system']}
    """
    
    await message.answer(text, parse_mode='HTML')
```

**Результат**: Статистика доступна

---

### День 10: Экспорт и интеграции

#### Задача 2.6: Экспорт лидов в CSV
**Время**: 1 час

**Чеклист**:
- [ ] Команда /export
- [ ] Выгрузка из БД
- [ ] Генерация CSV
- [ ] Отправка файла владельцу

**Код**:
```python
import csv
from io import StringIO

@router.message(Command("export"))
async def cmd_export(message: Message):
    if message.from_user.id != OWNER_ID:
        return
    
    leads = await get_all_leads_from_db()
    
    output = StringIO()
    writer = csv.DictWriter(output, fieldnames=['id', 'name', 'phone', 'bottleneck', 'comment', 'created_at'])
    writer.writeheader()
    writer.writerows(leads)
    
    file = BufferedInputFile(
        output.getvalue().encode('utf-8'),
        filename=f"leads_{datetime.now().strftime('%Y%m%d')}.csv"
    )
    
    await message.answer_document(file, caption="📊 Экспорт лидов")
```

**Результат**: Экспорт работает

---

#### Задача 2.7: Интеграция с Google Sheets (опционально)
**Время**: 3 часа

**Чеклист**:
- [ ] Создать Google Sheet
- [ ] Настроить Google Service Account
- [ ] Установить `gspread`, `oauth2client`
- [ ] Функция добавления лида в таблицу

**Результат**: Лиды попадают в Google Sheets

---

## SPRINT 3: Улучшения и тестирование (2 дня)

### День 11: UX-улучшения

#### Задача 3.1: Typing action
**Время**: 1 час

**Чеклист**:
- [ ] Добавить `await bot.send_chat_action(chat_id, 'typing')` перед длинными сообщениями
- [ ] Небольшие задержки между сообщениями

**Результат**: Бот выглядит "живым"

---

#### Задача 3.2: Обработка ошибок
**Время**: 2 часа

**Чеклист**:
- [ ] Try-except блоки
- [ ] Логирование ошибок
- [ ] Сообщения пользователю при ошибках

**Код**:
```python
import logging

logger = logging.getLogger(__name__)

try:
    # код
except Exception as e:
    logger.error(f"Error: {e}")
    await message.answer("⚠️ Произошла ошибка. Попробуйте /start")
```

**Результат**: Ошибки обрабатываются

---

#### Задача 3.3: Кнопка "Начать заново"
**Время**: 1 час

**Чеклист**:
- [ ] Кнопка "Пройти заново" в конце
- [ ] Очистка state
- [ ] Возврат к /start

**Результат**: Можно переделать квиз

---

### День 12: Тестирование

#### Задача 3.4: Функциональное тестирование
**Время**: 3 часа

**Чеклист**:
- [ ] Тест: /start → приветствие
- [ ] Тест: прохождение всех вопросов
- [ ] Тест: все 4 варианта результатов
- [ ] Тест: заполнение лид-формы
- [ ] Тест: пропуск комментария
- [ ] Тест: некорректный телефон
- [ ] Тест: уведомление владельцу
- [ ] Тест: /stats
- [ ] Тест: /export

**Результат**: Всё работает

---

#### Задача 3.5: Тестирование на устройствах
**Время**: 1 час

**Чеклист**:
- [ ] Тест на iOS
- [ ] Тест на Android
- [ ] Тест на Desktop

**Результат**: UX хороший на всех платформах

---

## SPRINT 4: Деплой (1 день)

### День 13: Продакшен

#### Задача 4.1: Подготовка сервера
**Время**: 2 часа

**Чеклист**:
- [ ] Арендовать VPS (DigitalOcean/Hetzner)
- [ ] SSH доступ
- [ ] Установить Python 3.10+
- [ ] Установить PostgreSQL
- [ ] Создать БД

**Результат**: Сервер готов

---

#### Задача 4.2: Деплой кода
**Время**: 2 часа

**Чеклист**:
- [ ] Склонировать репозиторий на сервер
- [ ] Создать venv
- [ ] Установить зависимости
- [ ] Создать .env с продакшен-настройками
- [ ] Запустить миграции БД

**Результат**: Код на сервере

---

#### Задача 4.3: Настройка автозапуска
**Время**: 1 час

**Чеклист**:
- [ ] Создать systemd service
- [ ] Включить автозапуск
- [ ] Проверить работу

**Файл**: `/etc/systemd/system/quiz_bot.service`
```ini
[Unit]
Description=Quiz Bot
After=network.target

[Service]
Type=simple
User=yourusername
WorkingDirectory=/home/yourusername/quiz_bot
ExecStart=/home/yourusername/quiz_bot/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

**Команды**:
```bash
sudo systemctl enable quiz_bot
sudo systemctl start quiz_bot
sudo systemctl status quiz_bot
```

**Результат**: Бот работает 24/7

---

#### Задача 4.4: Мониторинг и логи
**Время**: 1 час

**Чеклист**:
- [ ] Настроить rotation логов
- [ ] Настроить алерты при падении
- [ ] Бэкап БД

**Результат**: Мониторинг работает

---

## ИТОГОВЫЙ ЧЕКЛИСТ

### Перед запуском:
- [ ] Все тексты проверены на опечатки
- [ ] Логика скоринга работает корректно
- [ ] Все кнопки кликабельны
- [ ] Лид-форма сохраняет данные
- [ ] Уведомления владельцу приходят
- [ ] /stats показывает корректные данные
- [ ] Бот работает на сервере 24/7
- [ ] Бэкап БД настроен
- [ ] Документация готова

### После запуска:
- [ ] Отправить ссылку на бота эксперту
- [ ] Провести тестовый прогон с экспертом
- [ ] Собрать первый фидбек
- [ ] Подготовить промо-материалы для анонса

---

## РАСШИРЕННЫЙ ТАЙМЛАЙН

| День | Задачи | Часы |
|------|--------|------|
| 1 | Сценарий, контент, ниша | 8 |
| 2 | Техническая подготовка | 4 |
| 3 | Конфигурация, БД, модели | 8 |
| 4 | Старт, регистрация | 6 |
| 5 | Логика квиза | 8 |
| 6 | Обработка ответов, скоринг | 8 |
| 7 | Результаты, твист | 6 |
| 8 | Лид-форма | 8 |
| 9 | Уведомления, админка | 6 |
| 10 | Экспорт, интеграции | 8 |
| 11 | UX-улучшения | 6 |
| 12 | Тестирование | 8 |
| 13 | Деплой | 8 |

**ИТОГО**: ~90 часов (11-13 дней при работе по 7-8 часов)

---

## ПРИОРИТЕТЫ

### MUST HAVE (критично):
1. Старт и регистрация
2. Квиз с 5-7 вопросами
3. Скоринг и результаты
4. Лид-форма
5. Уведомления владельцу

### SHOULD HAVE (важно):
6. Статистика
7. Экспорт лидов
8. Обработка ошибок
9. UX-улучшения

### COULD HAVE (опционально):
10. Google Sheets интеграция
11. CRM-интеграция
12. A/B тесты
13. Мультисценарность

---

## КОНТАКТЫ

Если возникают вопросы по любой задаче — пишите!

🚀 Цель: Запуститься до праздников и принести клиенту первые заявки.
